# ============================================================================
# BACKEND ENVIRONMENT VARIABLES
# ============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to version control!

# ============================================================================
# MongoDB Configuration
# ============================================================================
# For local development: mongodb://localhost:27017
# For MongoDB Atlas (production): mongodb+srv://username:password@cluster.mongodb.net/
# For Railway MongoDB: Use the connection string from Railway dashboard
MONGO_URL=mongodb://localhost:27017
DB_NAME=breast_cancer_dashboard

# ============================================================================
# CORS Configuration
# ============================================================================
# Comma-separated list of allowed origins
# For local development:
CORS_ORIGINS=http://localhost:3000,http://localhost:8000
# For production (update with your actual frontend URL):
# CORS_ORIGINS=https://your-frontend-app.vercel.app,https://your-backend-app.railway.app

# ============================================================================
# OpenAI / OpenRouter Configuration
# ============================================================================
# IMPORTANTE: Este proyecto usa OpenRouter, NO OpenAI directamente
# Get your API key from: https://openrouter.ai/keys
# La API key debe empezar con: sk-or-v1-
# Required for AI-powered insights and report generation
OPENAI_API_KEY=sk-or-v1-your_openrouter_api_key_here

# ⚠️ SEGURIDAD: NUNCA incluyas tu API key real en archivos que se suban a Git
# Configura esta variable SOLO en:
# - Archivo .env local (bloqueado por .gitignore)
# - Railway Dashboard → Variables (para producción)

# ============================================================================
# Server Configuration
# ============================================================================
# Host and port for the FastAPI server
# For Railway deployment, PORT is automatically set by Railway
HOST=0.0.0.0
PORT=8000

# ============================================================================
# JWT Authentication Configuration
# ============================================================================
# Secret key for JWT token signing (MUST be changed in production!)
# Generate a secure key with: openssl rand -hex 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-use-openssl-rand-hex-32

# ============================================================================
# Optional: Logging Configuration
# ============================================================================
# LOG_LEVEL=INFO
# DEBUG=False
